export const DAOTokens = Object.freeze({
    USER: 'user-module.user.dao',
});

export const RepoTokens = Object.freeze({
    MAILER: 'mailer-module.mailer.repo',
});

export const MapperTokens = Object.freeze({
    MAILER: 'mailer-module.mailer.mapper',

    USER: 'user-module.user.mapper',
});

export const UsecaseTokens = Object.freeze({
    SEND_VERIFICATION_MAILER: 'mailer-module.send-verification-mail.usecase',
    VERIFY_MAILER: 'mailer-module.verify-mail.usecase',
});

export const ProviderTokens = Object.freeze({
    MAILTRAP: 'mailtrap.provider',
});

export const MailerTokens = Object.freeze({
    QUERY_UTIL: 'mailer-module.query-util',
});
